server:
  port: 80
  servlet:
    context-path: /vizha

spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

  datasource:
    url: jdbc:postgresql://automotive.cxqw2yko4uas.ap-south-1.rds.amazonaws.com:5432/postgres?currentSchema=public
    username: Mr_Automotive
    password: Automotive#Enthusiast#2000#
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        # dialect: org.hibernate.dialect.PostgreSQLDialect  # optional